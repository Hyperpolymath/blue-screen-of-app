<!DOCTYPE html>
<html lang="<%= lang %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Windows</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background: #00579e;
            color: white;
            font-family: 'Lucida Console', 'Courier New', monospace;
            height: 100vh;
            padding: 40px;
            overflow: hidden;
        }

        .container {
            max-width: 100%;
        }

        .header {
            font-size: 18px;
            margin-bottom: 30px;
            border-bottom: 1px solid white;
            padding-bottom: 10px;
        }

        .message {
            font-size: 16px;
            line-height: 1.6;
            margin-bottom: 30px;
        }

        .problem {
            margin-top: 30px;
            margin-bottom: 20px;
            font-size: 16px;
        }

        .stop-code-section {
            margin-top: 40px;
            margin-bottom: 20px;
        }

        .stop-code {
            font-size: 18px;
            font-weight: bold;
            margin-bottom: 10px;
            letter-spacing: 2px;
        }

        .technical {
            font-size: 14px;
            margin-top: 30px;
            opacity: 0.9;
        }

        .instructions {
            margin-top: 40px;
            font-size: 14px;
            line-height: 1.8;
        }

        .memory-dump {
            margin-top: 30px;
            font-size: 12px;
            opacity: 0.8;
        }

        .progress {
            margin-top: 30px;
            font-size: 14px;
        }

        .progress-text {
            margin-bottom: 10px;
        }

        .progress-bar {
            width: 300px;
            height: 20px;
            border: 1px solid white;
            padding: 2px;
            display: inline-block;
        }

        .progress-fill {
            height: 100%;
            background: white;
            transition: width 0.5s ease;
        }

        .qr-section {
            position: fixed;
            bottom: 30px;
            right: 30px;
            text-align: center;
            font-family: 'Segoe UI', sans-serif;
        }

        .qr-code {
            background: white;
            padding: 8px;
            margin-bottom: 8px;
        }

        .qr-code img {
            display: block;
        }

        .qr-message {
            font-size: 12px;
            max-width: 180px;
        }

        @media (max-width: 768px) {
            body {
                padding: 20px;
                font-size: 14px;
            }

            .header {
                font-size: 16px;
            }

            .message {
                font-size: 14px;
            }

            .stop-code {
                font-size: 16px;
            }

            .progress-bar {
                width: 200px;
            }

            .qr-section {
                position: static;
                margin-top: 30px;
            }
        }

        /* Blinking cursor effect */
        @keyframes blink {
            0%, 50% { opacity: 1; }
            51%, 100% { opacity: 0; }
        }

        .cursor {
            display: inline-block;
            width: 10px;
            height: 16px;
            background: white;
            margin-left: 5px;
            animation: blink 1s infinite;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            *** STOP: <%= stopCode %>
        </div>

        <div class="message">
            <%= description %>
        </div>

        <div class="problem">
            Technical Information:
        </div>

        <div class="stop-code-section">
            <div class="stop-code">
                *** STOP: 0x0000007B (<%= stopCode %>)
            </div>
        </div>

        <div class="technical">
            <%= technicalDetail %>
        </div>

        <div class="instructions">
            If this is the first time you've seen this Stop error screen,<br>
            restart your computer. If this screen appears again, follow<br>
            these steps:<br>
            <br>
            Check to make sure any new hardware or software is properly installed.<br>
            If this is a new installation, ask your hardware or software manufacturer<br>
            for any Windows updates you might need.<br>
            <br>
            If problems continue, disable or remove any newly installed hardware<br>
            or software. Disable BIOS memory options such as caching or shadowing.<br>
            If you need to use Safe Mode to remove or disable components, restart<br>
            your computer, press F8 to select Advanced Startup Options, and then<br>
            select Safe Mode.
        </div>

        <div class="progress">
            <div class="progress-text">
                Collecting error information: <%= percentage %>%
            </div>
            <div class="progress-bar">
                <div class="progress-fill" style="width: <%= percentage %>%"></div>
            </div>
            <span class="cursor"></span>
        </div>

        <div class="memory-dump">
            Beginning dump of physical memory<br>
            Dumping physical memory to disk: <%= percentage %>
        </div>
    </div>

    <% if (qrCode) { %>
    <div class="qr-section">
        <div class="qr-code">
            <img src="<%= qrCode %>" alt="QR Code">
        </div>
        <div class="qr-message">
            <%= qrMessage %>
        </div>
    </div>
    <% } %>

    <script>
        // Prevent scrolling
        document.addEventListener('touchmove', function(e) {
            e.preventDefault();
        }, { passive: false });

        // Slowly increase progress
        let currentPercentage = <%= percentage %>;
        const progressFill = document.querySelector('.progress-fill');
        const progressText = document.querySelector('.progress-text');
        const memoryDump = document.querySelector('.memory-dump');

        setInterval(() => {
            if (currentPercentage < 100 && Math.random() > 0.3) {
                currentPercentage = Math.min(100, currentPercentage + 1);
                progressFill.style.width = currentPercentage + '%';
                progressText.textContent = 'Collecting error information: ' + currentPercentage + '%';
                memoryDump.innerHTML = 'Beginning dump of physical memory<br>Dumping physical memory to disk: ' + currentPercentage;
            }
        }, 1000);
    </script>
</body>
</html>
